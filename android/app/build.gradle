plugins {
    id "com.android.application"
}

android {
    namespace "com.byw.roadofpoker"
    compileSdk 35

    defaultConfig {
        applicationId "com.byw.roadofpoker"
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
